title: Flight Card Pro Dev
views:
  - title: Card Testing
    path: testing
    icon: mdi:airplane
    cards:
      - type: markdown
        content: |
          # Flight Card Pro Testing
          Use the controls below to simulate different flight states and test the card.

      - type: tile
        entity: input_select.payload_test_options
        features_position: bottom
        vertical: false
        features:
          - type: select-options

      - type: custom:flight-card-pro
        entity: sensor.payload
